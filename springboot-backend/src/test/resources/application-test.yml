spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:tms;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:

jwt-secret: test-only-jwt-secret-not-for-production
log-dir: target/test-logs
